<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= isEdit ? 'Edit Task' : 'New Task' %> • Todoist</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar', { user: typeof user !== 'undefined' ? user : null }) %>

  <div class="todoform-wrapper">
    <div class="todoform-stars">
      <div class="star" style="top: 20%; left: 10%;"></div>
      <div class="star" style="top: 40%; left: 30%;"></div>
      <div class="star" style="top: 60%; left: 70%;"></div>
      <div class="star" style="top: 80%; left: 20%;"></div>
      <div class="star" style="top: 30%; left: 80%;"></div>
      <div class="star" style="top: 50%; left: 50%;"></div>
    </div>

    <div class="todoform-container">
      <div class="todoform-card">
        <div class="todoform-header">
          <div class="todoform-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <% if (isEdit) { %>
                <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
              <% } else { %>
                <line x1="12" y1="5" x2="12" y2="19"></line>
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <circle cx="12" cy="12" r="10"></circle>
              <% } %>
            </svg>
          </div>
          <h1 class="todoform-title">
            <%= isEdit ? "Refine Your Task ✏️" : "Plan Your Focus ✨" %>
          </h1>
          <p class="todoform-subtitle">
            <%= isEdit
              ? "Small improvements compound into big wins."
              : "Small tasks done daily create massive momentum." %>
          </p>
        </div>

        <form 
          class="todoform-form" 
          method="POST" 
          action="<%= isEdit ? `/todos/${todo._id}?_method=PATCH` : '/todo' %>"
        >
          <div class="todoform-form-group">
            <label for="task" class="todoform-label">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="9 11 12 14 22 4"></polyline>
                <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"></path>
              </svg>
              Task
            </label>
            <input
              type="text"
              id="task"
              name="task"
              placeholder="What do you want to accomplish?"
              value="<%= todo?.task || '' %>"
              class="todoform-input"
              required
            />
          </div>

          <div class="todoform-row">
            <div class="todoform-form-group">
              <label for="priority" class="todoform-label">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"></path>
                </svg>
                Priority
              </label>
              <select id="priority" name="priority" class="todoform-select" required>
                <option value="" disabled <%= !todo ? "selected" : "" %>>
                  Select priority
                </option>
                <option value="High" <%= todo?.priority === "High" ? "selected" : "" %>>
                  🔴 High
                </option>
                <option value="Medium" <%= todo?.priority === "Medium" ? "selected" : "" %>>
                  🟡 Medium
                </option>
                <option value="Low" <%= todo?.priority === "Low" ? "selected" : "" %>>
                  🟢 Low
                </option>
              </select>
            </div>

            <div class="todoform-form-group">
              <label for="dueDate" class="todoform-label">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                  <line x1="16" y1="2" x2="16" y2="6"></line>
                  <line x1="8" y1="2" x2="8" y2="6"></line>
                  <line x1="3" y1="10" x2="21" y2="10"></line>
                </svg>
                Due Date
              </label>
              <input
                type="date"
                id="dueDate"
                name="dueDate"
                value="<%= todo?.dueDate ? todo.dueDate.toISOString().split('T')[0] : '' %>"
                class="todoform-input"
              />
            </div>
          </div>

          <div class="todoform-actions">
            <a href="/todos" class="todoform-cancel-btn">Cancel</a>
            <button type="submit" class="todoform-submit-btn">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <% if (isEdit) { %>
                  <polyline points="20 6 9 17 4 12"></polyline>
                <% } else { %>
                  <line x1="12" y1="5" x2="12" y2="19"></line>
                  <line x1="5" y1="12" x2="19" y2="12"></line>
                <% } %>
              </svg>
              <%= isEdit ? "Update Task" : "Add Task" %>
            </button>
          </div>
        </form>

        <div class="todoform-motivation">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
            <polyline points="22 4 12 14.01 9 11.01"></polyline>
          </svg>
          Discipline today = freedom tomorrow.
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Your Todo List</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <style>
      :root {
        --bg: #020617;
        --card: #111827;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2937;
        --high: #ef4444;
        --medium: #f59e0b;
        --low: #22c55e;
      }

      * {
        box-sizing: border-box;
        font-family: Inter, system-ui, sans-serif;
      }

      body {
        margin: 0;
        background: var(--bg);
        color: var(--text);
        min-height: 100vh;
        padding: 30px;
      }

      .container {
        max-width: 800px;
        margin: auto;
      }

      h1 {
        font-size: 2rem;
        margin-bottom: 6px;
      }

      .subtitle {
        color: var(--muted);
        margin-bottom: 28px;
      }

      .todo-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 14px;
        padding: 18px;
        margin-bottom: 14px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }

      .left {
        display: flex;
        gap: 14px;
        align-items: center;
      }

      .left input {
        width: 18px;
        height: 18px;
        accent-color: #22c55e;
      }

      .task {
        font-weight: 600;
        margin-bottom: 4px;
      }

      .meta {
        font-size: 0.8rem;
        color: var(--muted);
      }

      .badge {
        padding: 4px 10px;
        border-radius: 999px;
        font-size: 0.75rem;
        font-weight: 600;
        color: #020617;
      }

      .high {
        background: var(--high);
      }
      .medium {
        background: var(--medium);
      }
      .low {
        background: var(--low);
      }

      .important {
        border: 1px solid #22c55e;
        color: #22c55e;
        padding: 4px 8px;
        border-radius: 8px;
        font-size: 0.7rem;
        margin-left: 6px;
      }

      .empty {
        text-align: center;
        margin-top: 60px;
        color: var(--muted);
      }

      .add-btn {
        display: inline-block;
        margin-bottom: 24px;
        text-decoration: none;
        background: linear-gradient(135deg, #22c55e, #16a34a);
        color: #020617;
        padding: 12px 18px;
        border-radius: 10px;
        font-weight: 700;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>Your Focus Board 🧠</h1>
      <div class="subtitle">Execute daily. Progress compounds.</div>

      <a href="/todos/new" class="add-btn">+ Add New Task</a>

      <% if (todos.length === 0) { %>
      <div class="empty">No tasks yet. Start small. Win daily.</div>
      <% } %> <% todos.forEach(todo => { %>
      <div class="todo-card">
        <div class="left">
          <form method="POST" action="/todos/<%= todo._id %>/toggle">
            <input type="checkbox" onchange="this.form.submit()" <%=
            todo.completed ? "checked" : "" %> />
          </form>

          <div>
            <div class="task">
              <%= todo.task %> <% if (todo.important) { %>
              <span class="important">IMPORTANT</span>
              <% } %>
            </div>

            <% const dueDate = todo.dueDate ? new
            Date(todo.dueDate).toDateString() : "No deadline"; %>

            <div class="meta">Due: <%= dueDate %></div>
          </div>
        </div>

        <span class="badge <%= (todo.priority || 'low').toLowerCase() %>">
          <%= todo.priority || 'Low' %>
        </span>
      </div>
      <% }) %>
      <script src="../public/js/todos.js"></script>
    </div>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Todos • Todoist</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar', { user: typeof user !== 'undefined' ? user : null }) %>

  <div class="todos-wrapper">
    <div class="todos-container">
      <div class="todos-header">
        <div class="todos-header-content">
          <h1 class="todos-main-title">Your Focus Board 🧠</h1>
          <p class="todos-subtitle">Execute daily. Progress compounds.</p>
        </div>
        <a href="/todos/new" class="todos-add-btn">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="12" y1="5" x2="12" y2="19"></line>
            <line x1="5" y1="12" x2="19" y2="12"></line>
          </svg>
          Add New Task
        </a>
      </div>  

      <% if (todos.length === 0) { %>
        <div class="todos-empty">
          <div class="todos-empty-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="9 11 12 14 22 4"></polyline>
              <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"></path>
            </svg>
          </div>
          <h3 class="todos-empty-title">No tasks yet</h3>
          <p class="todos-empty-text">Start small. Win daily.</p>
          <a href="/todos/new" class="todos-empty-btn">Create your first task</a>
        </div>
      <% } else { %>
        <div class="todos-grid">
          <% todos.forEach(todo => { %>
            <div class="todos-card <%= todo.completed ? 'todos-card-completed' : '' %>">
              <div class="todos-card-header">
                <form method="POST" action="/todos/<%= todo._id %>/toggle" class="todos-checkbox-form">
                  <input 
                    type="checkbox" 
                    class="todos-checkbox"
                    onchange="this.form.submit()" 
                    <%= todo.completed ? "checked" : "" %> 
                  />
                </form>

                <div class="todos-card-content">
                  <h3 class="todos-task-title <%= todo.completed ? 'todos-task-completed' : '' %>">
                    <%= todo.task %>
                  </h3>
                  <% const dueDate = todo.dueDate ? new Date(todo.dueDate).toDateString() : "No deadline"; %>
                  <p class="todos-task-date">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                      <line x1="16" y1="2" x2="16" y2="6"></line>
                      <line x1="8" y1="2" x2="8" y2="6"></line>
                      <line x1="3" y1="10" x2="21" y2="10"></line>
                    </svg>
                    <%= dueDate %>
                  </p>
                </div>
              </div>

              <div class="todos-card-footer">
                <span class="todos-badge todos-badge-<%= (todo.priority || 'low').toLowerCase() %>">
                  <%= todo.priority || 'Low' %>
                </span>

                <div class="todos-actions">
                  <a href="/todos/<%= todo._id %>/edit" class="todos-action-btn todos-edit-btn" data-id="<%= todo._id %>">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                      <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                    </svg>
                  </a>
                  <button class="delete-btn todos-action-btn" data-id="<%= todo._id %>">
                    <svg class="delete-btn" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <polyline points="3 6 5 6 21 6"></polyline>
                      <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
                      <line x1="10" y1="11" x2="10" y2="17"></line>
                      <line x1="14" y1="11" x2="14" y2="17"></line>
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>

  <%- include('partials/footer') %>
  <script src="/js/todos.js"></script>
  <script src="/js/delete.js"></script>
</body>
</html>